@{
    Layout = "_EvalLayout.cshtml";
    Page.Title = "Submit Evaluation";
    var db = Database.Open("omesEvals");

    var evalId = Request.QueryString["evalID"];
}

<nav class="saveNav">
    <a class="btn btn-outline-dark btn-block btn-lg dashBtn" href="SaveEval?evalID=@evalId">Submit Evaluation</a>
</nav>
