@{
    Layout = "_EmptyTemplateLayout.cshtml";
    Page.Title = "Review Evaluation";
    var db = Database.Open("omesEvals");

    var evalID = Request.Form["evaluations"];
}
<nav class="saveNav">
    <a class="btn btn-outline-dark btn-block btn-lg dashBtn" href="ConfirmReviewEval?evalID=@evalID">Review Evaluation</a>
</nav>